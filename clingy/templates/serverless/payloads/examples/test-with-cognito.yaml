# Test payload with Latam Cognito authentication

routeKey: "GET /test"
rawPath: "/dev/test"

# Add Latam authentication
requestContext:
  authorizer:
    lambda:
      db_schema: "latam_test"
      db_secret_arn: "arn:aws:secretsmanager:us-west-2:499799893363:secret:prod/xsi/db-uf1tPP"
      email: "auth@latam.com"
      family_name: "dev"
      name: "latam"
      xsi_client: "latam"
      xsi_role: "netsuite"
  http:
    path: "/dev/test"

body: ""
